package com.chery.exeed.crm.lead.mapper;

import com.chery.exeed.crm.lead.model.MetaKnowChannel;

public interface MetaKnowChannelMapper {
    int insert(MetaKnowChannel record);

    int insertSelective(MetaKnowChannel record);
}